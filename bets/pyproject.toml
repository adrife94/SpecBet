[project]
name = "betting-cli"
version = "0.1.0"
description = "Herramienta de apoyo a decisiones de apuestas deportivas (SDD)."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
